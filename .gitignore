node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*

# Built Mac app and disk images — the release download is built on demand,
# never committed. See the README for the build and packaging commands.
*.dmg

# Firebase CLI deploy cache — rewritten on every deploy
.firebase/

# Xcode per-user window and editor state
xcuserdata/
*.xcuserstate
