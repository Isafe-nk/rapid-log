node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*

# Locally built disk images, which land in macos/build/artifacts. The one in
# public/ is the download itself and has to be committed, hence the exception.
# See CONTRIBUTING.md for the build commands.
*.dmg
!public/RapidLog-macOS.dmg

# Firebase CLI deploy cache — rewritten on every deploy
.firebase/

# Xcode per-user window and editor state
xcuserdata/
*.xcuserstate
