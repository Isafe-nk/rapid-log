node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*

# Locally built disk images. The zip at public/RapidLog-macOS.zip is committed
# for now, but releases rebuild both artifacts from source, so nothing built
# here is worth keeping. See CONTRIBUTING.md for the build commands.
*.dmg

# Firebase CLI deploy cache — rewritten on every deploy
.firebase/

# Xcode per-user window and editor state
xcuserdata/
*.xcuserstate
